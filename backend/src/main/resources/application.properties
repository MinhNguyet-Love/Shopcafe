# =======================================================
# SHOPCAFE BACKEND CONFIGURATION (no emoji)
# =======================================================

# JWT Configuration
app.jwt.secret=my-super-secret-key-for-shopcafe
app.jwt.expiration-seconds=86400

# MongoDB Atlas Configuration
spring.data.mongodb.uri=mongodb+srv://minhnguyet22072004_db_user:PLyCuKheHmYcSJuP@cafeshop.6ggcnov.mongodb.net/cafeshopdb?retryWrites=true&w=majority
spring.data.mongodb.database=cafeshopdb

# Server Configuration
server.port=8080
spring.application.name=shopcafe-backend

# TLS Configuration
jdk.tls.client.protocols=TLSv1.2
javax.net.ssl.trustStoreType=JKS

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.shopcafe.backend=DEBUG

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:5173,http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
